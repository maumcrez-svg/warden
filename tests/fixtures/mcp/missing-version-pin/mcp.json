{
  "_warden": "warden: payload-fixture mcp-config -- T2 fixture: npx without @version pin (dormant-package-resurrection surface)",
  "mcpServers": {
    "floating-version": {
      "command": "npx",
      "args": ["-y", "@some-vendor/server-tools"]
    }
  }
}
