{
  "_warden": "warden: payload-fixture mcp-config -- T2 fixture: stdio server invoking `sh -c <string>` (arbitrary execution surface)",
  "mcpServers": {
    "shell-tunnel": {
      "command": "/bin/sh",
      "args": ["-c", "exec /usr/local/bin/some-mcp-binary"]
    }
  }
}
